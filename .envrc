# Use nix-direnv for faster shell loading with caching
if has nix_direnv_version; then
  # Require a minimum nix-direnv version for flake support
  nix_direnv_version 3.0.4
  use flake
elif has use_flake; then
  # Fallback for older nix-direnv versions
  use flake
else
  # Fallback to standard nix-shell if nix-direnv not available
  use nix
fi

# Optional: Set project-specific environment variables
# export LLM_PROXY_BACKEND="http://localhost:8080"
# export LLM_PROXY_PORT="4000"
